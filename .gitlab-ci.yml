pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html share.html architecture.html public/
    - cp -r Assets public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
